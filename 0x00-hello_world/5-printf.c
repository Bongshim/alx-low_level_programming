#include <stdio.h>

/**
  * main - prints a character using printf 
  *
  * Return: returns 0
  */

int main(void)
{
	char str = "with proper grammar, but the outcome is a piece of art,";

	printf("%c\n", str);

	return (0);
}
