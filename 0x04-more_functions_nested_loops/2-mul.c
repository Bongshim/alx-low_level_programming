#include "main.h"

/**
*mul - function that checks for uppercase characters
*@a: int type number
*@b: int type number
*Return: returns the result
*/

int mul(int a, int b)
{
	int c;

	c = a * b;
	return (c);
}
